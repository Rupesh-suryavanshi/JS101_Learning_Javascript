var initialUsername = "Rupesh";
var initialPassword = "cat144";

var username = "Rupesh";
var password = "cat144";

if (initialUsername == username) {
  if (initialPassword == password) {
    console.log("log in");
  }
  else {
    console.log("Password missmatch");
  }
}
else {
  console.log("Username missmatch");
}